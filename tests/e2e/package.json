{
  "name": "openporte-e2e",
  "private": true,
  "description": "Browser-driven settings-matrix tests for OpenPorte (integrations x auto-mode x floating). See README.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
